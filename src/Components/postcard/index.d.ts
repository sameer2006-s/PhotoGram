import * as React from 'react';
import { DocRes } from '../../types';
interface IPostCardProps {
    data: DocRes;
}
declare const PostCard: React.FunctionComponent<IPostCardProps>;
export default PostCard;
